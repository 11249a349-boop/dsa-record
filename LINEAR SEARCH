Aim of Linear Search:
To search for a specific element (key) in a list or array by checking each element one by one until the desired element is found or the end of the list is reached.
Works on both sorted and unsorted arrays.
Simple but not the most efficient for large datasets.

PROGRAM:
#include <stdio.h>

int main() {
    int n, key, i, flag = 0;
    printf("Enter number of elements: ");
    scanf("%d", &n);
    
    int arr[n];
    printf("Enter elements:\n");
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    printf("Enter element to search: ");
    scanf("%d", &key);
    
    for(i = 0; i < n; i++) {
        if(arr[i] == key) {
            printf("Element found at position %d\n", i+1);
            flag = 1;
            break;
        }
    }
    
    if(flag == 0) {
        printf("Element not found\n");
    }
    
    return 0;
}
